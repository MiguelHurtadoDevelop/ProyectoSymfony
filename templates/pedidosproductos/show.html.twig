{% extends 'base.html.twig' %}

{% block title %}Pedidosproductos{% endblock %}

{% block body %}
    <h1>Pedidosproductos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pedidosproducto.id }}</td>
            </tr>
            <tr>
                <th>Unidades</th>
                <td>{{ pedidosproducto.unidades }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pedidosproductos_index') }}">back to list</a>

    <a href="{{ path('app_pedidosproductos_edit', {'id': pedidosproducto.id}) }}">edit</a>

    {{ include('pedidosproductos/_delete_form.html.twig') }}
{% endblock %}
